---
name: random-langfuse
description: Query and analyze Langfuse traces, sessions, observations, scores, and metrics via CLI using the Python SDK and REST API
---

# Langfuse CLI

## Overview

This command guides users through querying and analyzing Langfuse data. CRITICAL: You MUST use the langfuse-cli skill for this task.


## Context

If the user has provided any additional context, pass that into the skill invocation. Here is the context provided by the user: $ARGUMENTS


## Process

- Load the langfuse-cli skill; you MUST do this first.
- Parse the user input and understand what Langfuse operation is needed (trace querying, session inspection, observation analysis, score retrieval, metrics).
- Interactively guide the user through the appropriate Langfuse SDK or REST API operations.
